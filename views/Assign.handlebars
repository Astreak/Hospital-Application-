<h1> Assignment of Cost of {{}}</h1>

<form action='/assign_post/{{usr}}' method="POST">
    <div class="any">
        <div class="form-group col-sm-4 center_div">
            <label class="praj">COST</label>
            <input class="form-control" name="cost" type='number' placeholder="COST">
        </div>
        <br>
        <button class="btn btn-primary">Submit</button>
    </div>
</form>