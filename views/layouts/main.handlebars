<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="bootstrap/dist/css/bootstrap.css">
    <link rel="stylesheet" href="stylesheets/main.css">
    
</head>
<style>

</style>

<body>
    <nav class="navbar navbar-light bg-light" style="font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif">
        <a href="#" class="navbar-brand" style="font-weight: 500;">Medical Application</a>
        {{# if user }}
            <ul class="navbar-nav ml-auto">
                

                <p class='logged'>
                {{# if status }}
                   <span style="color:rgb(12, 133, 255)">(Dr.){{user}}</span>
                {{ else }}
                    {{user}}
                {{/if}} 
                <p>
                </span>
            </ul>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav">
                    <li class="navbar-item">
                        <a class='navbar-link' href='/logout'>Logout</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="navbar-item">
                        {{# if status}}
                        <a class='navbar-link' href='/tasks'>Tasks</a>
                        {{ else }}
                        <a class='navbar-link' href='/treatment'>Treat Yourself</a>
                        {{/if}}
                    </li>
                </ul>
            </div>
        {{/if}}
        
    </nav>
    <div class='clock'></div>
    <br>
    <br>
        <h1> @Welcome To <span class="head">Distro</span> Hospital</h1>

</body>

</html>